# Tasks

## Todo

- Add `begin` and `end` Flow
- Add JSON schema into Flow
- Allow Flow to pass in schema to customize Action body
- Validate Action body to schema before call Flow
- Finish default Actions in Input for all Components
- Add default Actions in each component to run demos
- Add Walk

## Backlog

- Treeshake `@walker/react`
- Deploy `@walker/react` to npm
