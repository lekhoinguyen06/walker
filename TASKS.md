# Tasks

## Todo

- Use state for Input & Select demos
- Dispatch hover event for click flow
- Remove support for `content` and `raw` in Walker Element
- Provide ctx for mapper
- Detail logging
- Write v1 API tests
- Integrate Typedoc with Mintlify
- Deploy `@walker/core` v0.1.0 and `@walker/react` v0.1.0 to npm

## Backlog

- Pass flows' body schema to `useConciergeWalk`
- Add none flow for none walk purposes
- Add done flow for done walk purposes such as clean history, survey star, and cleanup callbacks
- Create `@vstaffs/sdk` by exporting Hono clients
- Authenticate Concierge Walk and Chat service using `@vstaffs/sdk`
- Create `@walker/cli` from `@vstaffs/sdk` to authenticate session before calling Concierge services
- Write Introduction Tuturial for React Playground and Landing page
