import { describe, it } from "vitest";

describe("runtime", () => {
  it("Flows' schema must derive from ActionSchema", () => {});

  it("The unioned Flows Schema must be JSONSchema", () => {});

  it("The unioned Flows Schema validate the inputFlow (from webFlows) correctly", () => {});

  it("The unioned Flows Schema validate the clickFlow (from webFlows) correctly", () => {});
});
