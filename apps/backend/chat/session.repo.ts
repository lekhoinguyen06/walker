const SessionRepository = {
    createSession: async ({ userId }: { userId: string; }) => {

    },

    querySession: async ({ userId, sessionId }: { userId: string; sessionId: string; }) => {
        
    },

    deleteSession: async ({ userId, sessionId }: { userId: string; sessionId: string; }) => {

    }
}

export default SessionRepository;